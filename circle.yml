machine:
  timezone: Asia/Tokyo
  php:
    version: 7.0.4
dependencies:
  override:
    - composer install
test:
  override:
    - phpenv global 5.3.3
    - vendor/bin/phpunit
    - phpenv global 5.4.37
    - vendor/bin/phpunit
    - phpenv global 5.5.21
    - vendor/bin/phpunit
    - phpenv global 5.6.14
    - vendor/bin/phpunit
    - phpenv global 7.0.4
    - vendor/bin/phpunit
